name = "metis-cron-scheduler"
main = "cron-worker.js"
compatibility_date = "2026-03-13"

[triggers]
crons = ["*/5 * * * *"]

[vars]
METIS_ORIGIN_URL = "https://example.org"

# Store the real secret with:
# wrangler secret put METIS_CRON_SECRET
