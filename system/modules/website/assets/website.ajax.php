<?php
/**
 * Website Module — AJAX asset stub
 *
 * The actual AJAX handlers are registered in ajax/website.ajax.php,
 * which bootstrap.php loads directly. This file satisfies the module
 * validator's assets/ajax check and is the enqueue target for the
 * metisWebsiteAjax JS object.
 */

if ( ! defined( 'METIS_ROOT' ) ) {
    exit;
}
